apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-1-17
  name: nginx
spec:
  type: NodePort
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 32080
  selector:
    app: nginx-1-17
