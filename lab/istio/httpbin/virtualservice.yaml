apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: httpbin
  namespace: dev
spec:
  hosts:
  - "httpbin.example.com"
  gateways:
  - ingress-gateway
  http:
  - route:
    - destination:
        host: httpbin
        port:
          number: 8000
