controller:
  metrics:
    enabled: true
    service:
      labels:
        prometheus.io/scrape: "true"

redis:
  metrics:
    enabled: true

server:
  metrics:
    enabled: true
    service:
      labels:
        prometheus.io/scrape: "true"

repoServer:
  metrics:
    enabled: true
    service:
      labels:
        prometheus.io/scrape: "true"

applicationSet:
  metrics:
    enabled: true

notifications:
  metrics:
    enabled: true
